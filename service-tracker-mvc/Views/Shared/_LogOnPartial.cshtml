@if(Request.IsAuthenticated) {
    <text>Welcome <strong title="@Roles.GetRolesForUser().Last()">@HttpContext.Current.GetUserDisplayName()</strong> | @Html.ActionLink("Logout", "Logout", "User")</text>
}
else { 
    @Html.ActionLink("Login", "Login", "User")
}
