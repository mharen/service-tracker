<?xml version="1.0"?>
<!-- Unfortunately, AppHarbor doesn't apply this transformation
     To work around this, the default web.config IS the release mode 
     version and the changes for DEBUG mode are applied in web.debug.config -->